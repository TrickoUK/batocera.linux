--- a/Makefile
+++ b/Makefile
@@ -140,10 +140,6 @@
       endif
    endif
 
-ifneq ($(findstring Linux,$(shell uname -s)),)
-   HAVE_CDROM = 1
-endif
-
 # OS X
 else ifeq ($(platform), osx)
    TARGET  := $(TARGET_NAME)_libretro.dylib
