diff --git a/package/batocera/core/batocera-system/Config.in b/package/batocera/core/batocera-system/Config.in
index 862da7b6cb..bf6406ba49 100644
--- a/package/batocera/core/batocera-system/Config.in
+++ b/package/batocera/core/batocera-system/Config.in
@@ -376,11 +376,18 @@ config BR2_PACKAGE_BATOCERA_SYSTEM
 												 BR2_PACKAGE_BATOCERA_TARGET_BCM2712
 
 	# screens
-	select BR2_PACKAGE_DMD_SIMULATOR
-	select BR2_PACKAGE_DMD_SIMULATOR_PLAYER
-	select BR2_PACKAGE_DMD_PLAY_RUST
-	select BR2_PACKAGE_LIBDMDUTIL # dependancy of vpinball, but vpinball is not everywhere
-	select BR2_PACKAGE_BATOCERA_BACKGLASS if (BR2_PACKAGE_XSERVER_XORG_SERVER || BR2_PACKAGE_BATOCERA_WAYLAND) && !BR2_PACKAGE_BATOCERA_TARGET_JH7110
+	# Disabled for this fork along with vpinball below (see the "Pinball"
+	# select further down): these are all vpinball-only (DMD/backglass
+	# display support), and vpinball itself is disabled - no interest in
+	# virtual pinball on this board. Also avoids pulling in webkitgtk
+	# (needed only by pywebview, needed only by BATOCERA_BACKGLASS), which
+	# is one of the single most expensive packages to compile in the
+	# whole build.
+	# select BR2_PACKAGE_DMD_SIMULATOR
+	# select BR2_PACKAGE_DMD_SIMULATOR_PLAYER
+	# select BR2_PACKAGE_DMD_PLAY_RUST
+	# select BR2_PACKAGE_LIBDMDUTIL # dependancy of vpinball, but vpinball is not everywhere
+	# select BR2_PACKAGE_BATOCERA_BACKGLASS if (BR2_PACKAGE_XSERVER_XORG_SERVER || BR2_PACKAGE_BATOCERA_WAYLAND) && !BR2_PACKAGE_BATOCERA_TARGET_JH7110
 	select BR2_PACKAGE_SDL2_TOUCH_TEST    # ALL
 
 	# swap
@@ -1576,13 +1583,18 @@ config BR2_PACKAGE_BATOCERA_CONSOLE_SYSTEMS
 										   BR2_PACKAGE_BATOCERA_TARGET_SM8750
 
 	# Pinball
-	select BR2_PACKAGE_VPINBALL			if BR2_PACKAGE_BATOCERA_TARGET_X86_64_ANY      || \
-	                                       BR2_PACKAGE_BATOCERA_TARGET_RK3588          || \
-										   BR2_PACKAGE_BATOCERA_TARGET_RK3588_SDIO     || \
-										   BR2_PACKAGE_BATOCERA_TARGET_RK3588_MAINLINE || \
-										   BR2_PACKAGE_BATOCERA_TARGET_SM8250          || \
-										   BR2_PACKAGE_BATOCERA_TARGET_SM8550          || \
-										   BR2_PACKAGE_BATOCERA_TARGET_SM8750
+	# Disabled for this fork: no interest in virtual pinball. Also drops
+	# libdmdutil/dmd-simulator/dmd-play-rust/BATOCERA_BACKGLASS above,
+	# and transitively webkitgtk (pulled in only via
+	# BATOCERA_BACKGLASS -> pywebview -> webkitgtk), one of the most
+	# expensive packages to compile in the whole build.
+	# select BR2_PACKAGE_VPINBALL			if BR2_PACKAGE_BATOCERA_TARGET_X86_64_ANY      || \
+	#                                       BR2_PACKAGE_BATOCERA_TARGET_RK3588          || \
+	# 									   BR2_PACKAGE_BATOCERA_TARGET_RK3588_SDIO     || \
+	# 									   BR2_PACKAGE_BATOCERA_TARGET_RK3588_MAINLINE || \
+	# 									   BR2_PACKAGE_BATOCERA_TARGET_SM8250          || \
+	# 									   BR2_PACKAGE_BATOCERA_TARGET_SM8550          || \
+	# 									   BR2_PACKAGE_BATOCERA_TARGET_SM8750
 
 	# Panasonic 3DO
 	select BR2_PACKAGE_LIBRETRO_OPERA    if !BR2_PACKAGE_BATOCERA_TARGET_BCM2835	&& \
